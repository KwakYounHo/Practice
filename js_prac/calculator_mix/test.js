let box = [];

let a = 1;
let b = 2;

box.push(a);
console.log(box.length);

/*
box = [1];
= 1
*/

// -------------------------------------------------

let test = [];

// 1 클릭
for (i=0; i<1; i++) {
  box.push[i];
  console.log(box.length);
  for (i=0; i<box.length;) {
    box.pop()
  }
}

//2 클릭
for (i=0; i<2; i++) {
  box.push[i];
  console.log(box.length);
  for (i=0; i<box.length;) {
    box.pop()
  }
}

// 3 클릭
for (i=0; i<3; i++) {
  box.push[i];
  console.log(box.length);
  for (i=0; i<box.length;) {
    box.pop()
  }
}